const formatNumber=(num)=>{return num.toString().replace(/\B(?=(\d{3})+(?!\d))/g,"'")};const extractNumber=(str)=>{const numStr=str.replace(/['\.]/g,'').match(/\d+/);return numStr?+numStr[0]:0};const extractUnit=(str)=>{const match=str.match(/[^\d\s.']+/);return match?' '+match[0]:''};function initCounters(){if(typeof gsap==='undefined'||typeof ScrollTrigger==='undefined'){return}
gsap.registerPlugin(ScrollTrigger);document.querySelectorAll('.counter').forEach((el)=>{const originalText=el.getAttribute('data-target')||el.textContent;const target=extractNumber(originalText);const unit=extractUnit(originalText);if(target<=0){return}
if(!el.hasAttribute('data-target')){el.setAttribute('data-target',originalText)}
const counter={value:0};ScrollTrigger.create({trigger:el,start:'top 80%',once:!0,onEnter:function(){gsap.to(counter,{value:target,duration:2.5,ease:'power2.out',onUpdate:function(){el.textContent=formatNumber(Math.ceil(counter.value))+unit},onComplete:function(){el.textContent=formatNumber(target)+unit}})}})})}
document.addEventListener('DOMContentLoaded',function(){initCounters();const articles=document.querySelectorAll('section.naechste-schritte article');const buttons=document.querySelectorAll('section.naechste-schritte .wp-block-button__link');articles[0].classList.add('active');buttons[0].classList.add('active');buttons.forEach((button,index)=>{button.addEventListener('click',()=>{if(articles[index]){console.log('Toggling article visibility for index:',index);console.log('Button clicked:',button);buttons.forEach((b,i)=>{if(i!==index){b.classList.remove('active')}});button.classList.toggle('active');articles.forEach((a,i)=>{if(i!==index){a.classList.remove('active')}});articles[index].classList.toggle('active')}})})})
;