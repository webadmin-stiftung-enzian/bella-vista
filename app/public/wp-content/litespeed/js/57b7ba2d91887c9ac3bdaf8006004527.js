if(typeof gsap!=='undefined'&&typeof Draggable!=='undefined'){gsap.registerPlugin(Draggable)}else{console.error('GSAP oder Draggable ist nicht verfügbar.')}
document.addEventListener('DOMContentLoaded',function(){const mapContainer=document.querySelector('svg#Ebene_1');const legendContainer=document.getElementById('legende');const mapElements=["bahnhof","primarschule","verwaltung","sport","lebensmittel","kirche","bellavista"];function removeAllHighlights(reason){console.log('[map] removeAllHighlights — reason:',reason);mapContainer?.querySelectorAll('.highlight').forEach(el=>el.classList.remove('highlight'));legendContainer?.closest('svg')?.querySelectorAll('.highlight').forEach(el=>el.classList.remove('highlight'))}
function addHighlight(element,elementInList){const wasActive=element?.classList.contains('highlight')||elementInList?.classList.contains('highlight');console.log('[map] addHighlight — wasActive:',wasActive,'| map:',element?.id,'| legend:',elementInList?.id);if(wasActive){removeAllHighlights('toggle-off')}else{removeAllHighlights('before-set');if(element)element.classList.add('highlight');if(elementInList)elementInList.classList.add('highlight');console.log('[map] highlight SET — map.highlight:',element?.classList.contains('highlight'),'| legend.highlight:',elementInList?.classList.contains('highlight'))}}
function findMatchingId(target){const selector=mapElements.flatMap(id=>[`#${id}`,`[data-name="${id}"]`]).join(',');const g=target.closest(selector);if(!g)return null;return g.dataset.name||g.id}
const markerElements=Object.fromEntries(mapElements.map(id=>[id,{map:mapContainer?.querySelector(`#${id}`)??mapContainer?.querySelector(`[data-name="${id}"]`)??null,legend:legendContainer?.querySelector(`#${id}`)??null,}]));function handleMarkerClick(id){const{map:element,legend:elementInList}=markerElements[id];addHighlight(element,elementInList)}
document.addEventListener('click',function(e){const legendSvgRoot=legendContainer?legendContainer.closest('svg'):null;if(legendSvgRoot?.contains(e.target)){console.log('[map] document click — inside legend, checking ID');const matchedId=findMatchingId(e.target);if(matchedId)handleMarkerClick(matchedId);return}
if(mapContainer?.contains(e.target)){console.log('[map] document click — inside mapContainer, ignoring natively');return}
console.log('[map] document click — outside, removing highlights | target:',e.target);removeAllHighlights('outside-click')});const figure=mapContainer?mapContainer.parentElement:null;if(typeof Draggable==='undefined'||!mapContainer){mapElements.forEach(id=>{const{map:element}=markerElements[id];if(!element)return;element.addEventListener('click',()=>handleMarkerClick(id))});return}
let lastClickTime=0;var draggable=Draggable.create(mapContainer,{type:'x',edgeResistance:0.9,inertia:!1,bounds:figure||undefined,cursor:'grab',activeCursor:'grabbing',zIndexBoost:!1,onClick(e){console.log('[map] Draggable onClick fired');const matchedId=findMatchingId(e.target);if(matchedId){handleMarkerClick(matchedId)}else{removeAllHighlights('map-background-click')}},});mapContainer.style.touchAction='pan-y'})
;